spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    url: jdbc:h2:mem:testdb
    password: sa
  config:
    import: ''
  cloud:
    config:
      enabled: 'false'
      import-check:
        enabled: 'false'
